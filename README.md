# SkillTools

## 背景

Claude Code、Cursor、OpenCode 等 AI 编程工具各自维护独立的 skills 目录，导致重复配置和维护成本。本方案使用符号链接，在一个地方管理技能，所有工具共享同一套配置。

## 功能

```
~/.agents/skills/          ← 源目录（维护一份）
    ├── commit/
    ├── pdf/
    └── review-pr/

         ↓ 软链接同步

~/.claude/skills/           → 各工具访问同一份
~/.cursor/skills/
~/.config/opencode/skills/
```

- **自动发现**：检测已安装的 AI 工具并同步
- **预览模式**：dry-run 查看将要执行的操作
- **智能清理**：自动删除失效的符号链接
- **安全跳过**：已存在的非软链文件不会被覆盖

## 使用

```bash
# 基本用法 - 自动同步所有已安装工具
./skill-sync.sh

# 预览模式 - 不实际执行
./skill-sync.sh -d

# 指定源目录
./skill-sync.sh -s ~/my-skills

# 同步到指定工具
./skill-sync.sh -t ~/.claude/skills
```

## 支持的工具

| 工具 | 路径 |
|------|------|
| Claude | `~/.claude/skills` |
| Cursor | `~/.cursor/skills` |
| OpenCode | `~/.config/opencode/skills` |
| OpenClaw | `~/.openclaw/skills` |
| Codex | `~/.codex/skills` |

## 选项

```
-s, --source DIR     源 skills 目录 (默认: ~/.agents/skills)
-t, --target DIR     单个目标工具的 skills 目录
-d, --dry-run        预览模式，不实际执行
-v, --verbose        显示详细日志
-h, --help           显示帮助信息
```

## 安装

```bash
# 1. 克隆仓库
git clone https://github.com/zzu-ken/SkillTools.git
cd SkillTools

# 2. 赋予执行权限
chmod +x skill-sync.sh

# 3. 运行
./skill-sync.sh
```

## 输出示例

```
═══════════════════════════════════════════════════
🤖 Skill Sync Tool
═══════════════════════════════════════════════════

🔍 源目录: /Users/ken/.agents/skills
🔍 发现 3 个技能
📦 发现目标: claude → ~/.claude/skills

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📂 目标: claude/skills
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✓ commit (已同步)
  🔗 pdf → /Users/ken/.agents/skills/pdf
  🔗 review-pr → /Users/ken/.agents/skills/review-pr

═══════════════════════════════════════════════════
📊 统计
═══════════════════════════════════════════════════
  🔗 创建: 2
  ⚠️  跳过: 1
  🗑️  删除: 0
  ❌ 错误: 0
  ───────────────────────────────────────────
  总计: 3 技能
═══════════════════════════════════════════════════
```

---

<div align="center">

### 📱 关注公众号

<br>

<img src="./img/wechat_qrcode.jpg" alt="公众号二维码" width="200">

<br>

**精选干货 • 应用推荐 • 趣味分享**

**有价值的分享 • 一眼即懂**

</div>
